# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.7.1] - 2026-09-05

### Fixed

- 去掉「≤8 / >8 张」包大小门禁；改为严格按会话 ID 归属入库
- 会话树扫描时，遇到显式其它 `conversation_id`/`chat_id` 的子树会切断，避免当前会话节点下挂的全局缓存整包进来

## [2.7.0] - 2026-09-05

### Fixed

- 刷新串会话根因：中大包（>8 张 `image_ori_raw` / 历史列表）只入库当前页面可见图；禁止 Fiber/Ready 全量回传内存 records
- 面板严格要求 `conversation_chat_id`/`page_chat_id` 等于当前会话，缺字段不再展示
- 保留：小包按会话树入库、无标记小包在已有本会话图或可见图时可续写，切换会话靠 DOM+Fiber 加载

## [2.6.9] - 2026-09-05

### Fixed

- 页面内捕获对齐油猴：去掉 IndexedDB 回填与 chain 扫图；JSON 入库规则与油猴一致
- Popup 筛选严格按 `conversation_id`，避免其它会话脏数据混进当前会话视图

## [2.6.8] - 2026-09-05

### Fixed

- 同会话一次生成多图（如 4 张）只扫到前几张：允许本会话已开始捕获后的流式续包入库
- Fiber 扫完页面上每一张会话图（不再被全局 10 次扫描上限截断），未匹配时持续强制补扫

## [2.6.7] - 2026-09-05

### Fixed

- 恢复旧会话识别：带当前会话标记的历史包按会话树正常入库；`/im/chain/single` 同时解析图片
- 切换旧会话时从本机库回填当前会话图片到右侧看板，并延长补扫窗口

## [2.6.6] - 2026-09-05

### Fixed

- 彻底禁止无会话标记的 creations 全量入库；JSON 仅在「含当前会话标记 + 与页面可见图同源」时才写入
- Fiber / 解析不再把内存里全部 records 冲刷进当前会话，避免橙/虎等其它会话图被一次性扫进 Popup

## [2.6.5] - 2026-09-05

### Fixed

- 去掉无 chatId 时的 creations pending 冲刷，避免其它接口图进「未命名会话」
- Fiber 仅入库与当前页面图片同源的 creations，并缩短向上扫描层数，减少串会话

## [2.6.4] - 2026-09-05

### Fixed

- 会话内去水印变慢：`creations` 缓存入库时同步改写页面图片数据；DOM 匹配补上 asset key；入库后立刻增强，并降低已有记录时的强制 Fiber 频率

## [2.6.3] - 2026-09-05

### Fixed

- 新对话出图：流式包不含 chatId 时也能先缓存 creations，URL 落地后立刻入库；不再把 message_id 误判成其它会话
- 页面已出现水印图但未匹配时强制快速 Fiber 补扫，减轻「先显示水印、两三秒后才检测到」的延迟

## [2.6.2] - 2026-09-05

### Fixed

- 视频入库严格按请求发起时的会话 ID 过滤；不再从任意 `JSON.parse` 扫 `fallback_api`，避免其它会话视频串进当前会话（并减少重复条目）

## [2.6.1] - 2026-09-05

### Fixed

- 先收缩再关闭时，悬浮球不再漂到画面中间，关闭后固定回右下角

## [2.6.0] - 2026-09-05

### Changed

- 视频捕获改为与油猴相同的页面拦截（`fallback_api`），**移除 `chrome.debugger` 权限**
- Popup 状态改为「已就绪」，去掉调试器重连

## [2.5.8] - 2026-09-04

### Changed

- 更新扩展图标为「十一木」品牌 stump 图标（16 / 48 / 128）

## [2.5.7] - 2026-09-04

### Changed

- 扩展显示名称改为「豆包AI生成图片与视频无感去水印」，`short_name` 为「豆包无感去水印」

## [2.5.6] - 2026-09-03

### Fixed

- 引用参考图时重复入库：按 byteimg 资源路径稳定键去重，引用/换签名 URL 不再新增记录

## [2.5.5] - 2026-09-03

### Fixed

- 引用参考图生成时重复入库：输入区参考图不再持久化，并按原图 URL 去重
- 刷新当前会话后 Popup 串入其它会话记录：无 chatId 的历史大包不再整包入库，仅允许流式 creations 片段

## [2.5.4] - 2026-09-03

### Fixed

- 页内面板 / Popup 图片尺寸显示过慢：优先使用接口元数据与页面已加载图片尺寸，不再等待后台拉取完整原图后才显示

## [2.5.3] - 2026-09-03

### Fixed

- 新建会话流式出图时面板/Popup 检测不到：生成包常不含 chatId，且 Fiber 只去水印不入库；现改为当前会话页可入库，并在 chatId 就绪后补扫

## [2.5.2] - 2026-09-03

### Changed

- 扩展图标更换为「十一木」树桩形象（16 / 48 / 128）

## [2.5.1] - 2026-09-03

### Fixed

- 扩展页 CSP 增加 `wasm-unsafe-eval`，修复 Service Worker 无法实例化 Wasm 导致视频地址解析失败

## [2.5.0] - 2026-09-03

### Changed

- 视频解密盐改为分片 Base64 + XOR，并由 Wasm 模块在运行时还原（提高阅读难度，非绝对保密）
- 新增 `opaque-material.js` 与 `opaque/xcodec.wasm`

## [2.4.6] - 2026-09-03

### Fixed

- 就绪提示每个豆包页只显示一次，打开 Popup 不再重复弹出

## [2.4.5] - 2026-09-03

### Changed

- 「支持作者」增加空心爱心图标，「全部下载」增加下载箭头图标

## [2.4.4] - 2026-09-03

### Changed

- 页内面板底部：左「支持作者」、右「全部下载」，去掉数量文案

## [2.4.3] - 2026-09-03

### Changed

- 页内面板底部「下载全部」改为「支持作者」
- 就绪提示改为页面水平+垂直居中

## [2.4.2] - 2026-09-03

### Removed

- 移除 Popup「启用去水印」总开关；扩展安装后默认始终启用

## [2.4.1] - 2026-09-03

### Added

- 豆包页加载成功后居中显示「插件已就绪 · 数据仅在本地处理」提示条

## [2.4.0] - 2026-09-03

### Added

- Popup 总开关「启用去水印」：关闭后停止图片 Hook、隐藏页内面板、断开视频 Debugger

### Changed

- 扩展显示名称改为「豆包图片视频去水印」，`short_name` 为 DoubaoWaterMark-Remover

## [2.3.2] - 2026-09-03

### Changed

- 扩展目录由 `extension/` 重命名为 `doubaoparser/`
- README 调整章节顺序，补充油猴安装说明

### Added

- `userscript/doubao-nomark-images.user.js`：Greasy Fork / Tampermonkey 图片无水印捕获与下载（视频仍需 Chrome 扩展）

### Notes

- 网页只响应媒体相关 DOM 变化；Fiber 扫描延后并缩小遍历范围
- Popup 分批渲染与缩略图懒加载
- 网页面板居中显示「加载成功」，最大高度约 320px
- 切换会话后短时间内识别尚未写入 `src` 的图片占位节点

### Fixed

- 图片记录必须包含原图字段，避免视频封面被误判为无水印图片

### Notes

- IndexedDB v4；图片与视频共用缓存上限（50 / 100 / 250 / 500 MB）

## [2.3.0] - 2026

### Added

- 图片与视频合并为同一扩展、同一 Popup、同一会话分类与缓存管理
- 豆包网页内嵌媒体面板（图片 / 视频 Tab）
- Popup：会话筛选、媒体类型筛选、批量下载、缓存整理
- 视频：Debugger 监听、最高画质、预览刷新临时地址、下载重试
